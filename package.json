{
  "name": "equihealth-access-and-navigator",
  "private": true,
  "version": "1.0.0",
  "description": "EquiHealth Access and Navigator - Monorepo for equitable healthcare navigation platform",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev --",
    "build": "npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist dist",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "clean": "npm --prefix frontend run clean"
  }
}
